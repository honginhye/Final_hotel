<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="room">

    <!-- ===============================
         1. 전체 객실 목록 조회
         =============================== -->
    <select id="selectRoomTypeList" resultType="RoomTypeDTO">

        SELECT
            rt.room_type_id,
            rt.room_name,
            rt.room_grade,
            rt.bed_type,
            rt.view_type,
            rt.base_price,
            ri.image_url
        FROM room_type rt
        LEFT JOIN room_image ri
            ON rt.room_type_id = ri.room_type_id
            AND ri.is_main = 'Y'
        ORDER BY rt.room_type_id DESC

    </select>


   <!-- ===============================
     2. 필터 검색 + 날짜 범위 재고 조회
     - 날짜 범위 내 최소 재고(MIN)를 구해서
       availableCount 로 내려줌
     =============================== -->
	<select id="selectRoomTypeByFilter" parameterType="map" resultType="RoomTypeDTO">
	
	    SELECT
	        <!-- room type -->
	        rt.room_type_id,     
	        rt.room_name,        
	        rt.room_grade,       
	        rt.bed_type,         
	        rt.view_type,        
	        rt.base_price,       
	  
	        <!-- room image -->
	        ri.image_url,      
	        
	        <!-- 날짜 범위 중 최소 가격 -->
	        MIN(
	            ROUND(
	                rt.base_price
	                * NVL((
	                    SELECT se.price_rate
	                    FROM SEASON se
	                    WHERE rs.stay_date BETWEEN se.start_date AND se.end_date
	                    ORDER BY se.priority DESC
	                    FETCH FIRST 1 ROWS ONLY
	                ), 1)
	                * NVL(wr.rate_multiplier, 1)
	            )
	        ) AS min_price,
	               
	        <!-- 날짜 범위 중 최소 재고 -->          
	        NVL(MIN(rs.available_count), 0) AS availableCount 
	
	    FROM room_type rt
	
	    <!-- 대표 이미지 조인 -->
	    LEFT JOIN room_image ri
	        ON rt.room_type_id = ri.room_type_id
	        AND ri.is_main = 'Y'
	
	    <!-- 재고 테이블 반드시 조인 -->
	    LEFT JOIN room_stock rs
	        ON rt.room_type_id = rs.room_type_id
	    
	    <!-- 요일별 가중치 조인 -->
	    LEFT JOIN weekday_rate wr
	       ON wr.fk_hotel_id = rt.fk_hotel_id
	       AND wr.day_of_week = TO_NUMBER(TO_CHAR(rs.stay_date, 'D'))
	
	    WHERE 1=1
	
	    <!-- ===============================
	         날짜 조건
	         체크인 이상 ~ 체크아웃 미만
	         =============================== -->
	    <if test="check_in != null and check_in != '' 
	              and check_out != null and check_out != ''">
	        
	         <![CDATA[
		        AND rs.stay_date >= TO_DATE(#{check_in}, 'YYYY-MM-DD')
		        AND rs.stay_date < TO_DATE(#{check_out}, 'YYYY-MM-DD')
	    	]]>
	
	    </if>
	
	    <!-- 호텔 필터 -->
	    <if test="hotel != null and hotel != ''">
	        AND rt.fk_hotel_id = #{hotel}
	    </if>
	
	    <!-- 객실 등급 -->
	    <if test="room_grade != null and room_grade != ''">
	        AND rt.room_grade = #{room_grade}
	    </if>
	
	    <!-- 침대 타입 -->
	    <if test="bed_type != null and bed_type != ''">
	        AND rt.bed_type = #{bed_type}
	    </if>
	
	    <!-- 전망 타입 -->
	    <if test="view_type != null and view_type != ''">
	        AND rt.view_type = #{view_type}
	    </if>
	
	    <!-- MIN 사용했으므로 GROUP BY 필수 -->
	    GROUP BY
	        rt.room_type_id,
	        rt.room_name,
	        rt.room_grade,
	        rt.bed_type,
	        rt.view_type,
	        rt.base_price,
	        ri.image_url
	
	    <!-- ===============================
	         정렬 조건(우측 정렬)
	         =============================== -->
	    <choose>
	        <when test="sort == 'priceAsc'">
	            ORDER BY rt.base_price ASC
	        </when>
	        <when test="sort == 'priceDesc'">
	            ORDER BY rt.base_price DESC
	        </when>
	        <otherwise>
	            ORDER BY rt.room_type_id DESC
	        </otherwise>
	    </choose>
	
	</select>


    <!-- ===============================
         3. 객실 상세 조회
         =============================== -->
    <select id="selectRoomDetail"
        parameterType="long"
        resultType="RoomTypeDTO">

	    SELECT
	        rt.room_type_id,
	        rt.fk_hotel_id      AS brand_id,
	        rt.room_name,
	        rt.room_grade,
	        rt.bed_type,
	        rt.view_type,
	        rt.base_price,
	        rt.room_size,
	        rt.max_capacity,
	        ri.image_url
	    FROM room_type rt
	    LEFT JOIN room_image ri
	        ON rt.room_type_id = ri.room_type_id
	        AND ri.is_main = 'Y'
	    WHERE rt.room_type_id = #{roomId}
	
	</select>
    
    
    <!-- 객실 상세조회 - 캐러셀 -->
      <select id="selectRoomImages"
        parameterType="long"
        resultType="string">

	    SELECT image_url
	    FROM room_image
	    WHERE room_type_id = #{value}
	    ORDER BY sort_order
	
	</select>
    

    <!-- ===============================
         4. 날짜별 가격 조회 (모달)
         =============================== -->
    <select id="selectCalendarPrice" parameterType="int" resultType="map">

	    SELECT
	        TO_CHAR(s.stay_date, 'YYYY-MM-DD') AS stay_date,
	
	        ROUND(
	            rt.base_price
	            * NVL((
	                SELECT se.price_rate
	                FROM SEASON se
	                WHERE s.stay_date BETWEEN se.start_date AND se.end_date
	                ORDER BY se.priority DESC
	                FETCH FIRST 1 ROWS ONLY
	            ), 1)
	            * NVL(wr.rate_multiplier, 1)
	        ) AS final_price,
	
	        s.available_count
	
	    FROM ROOM_STOCK s
	    JOIN ROOM_TYPE rt
	      ON s.room_type_id = rt.room_type_id
	
	    LEFT JOIN WEEKDAY_RATE wr
	      ON wr.fk_hotel_id = rt.fk_hotel_id
	     AND wr.day_of_week = TO_NUMBER(TO_CHAR(s.stay_date, 'D'))
	
	    WHERE s.room_type_id = #{value}
	      AND s.stay_date BETWEEN TRUNC(SYSDATE)
                    AND ADD_MONTHS(TRUNC(SYSDATE), 12)
	
	    ORDER BY s.stay_date	
	
	</select>
	
	
	<!-- 5. 비교 모달용 (비교함에 담긴 객실 id리스트를 기준으로 객실 정보 조회하기)  푸터 -->
	<select id="selectRoomsByIds" parameterType="list" resultType="RoomTypeDTO">
	    SELECT *
	    FROM room_type
	    WHERE room_type_id IN
	    <foreach collection="list" item="id" open="(" close=")" separator=",">
	        #{id}
	    </foreach>
	</select>
	
	
	<!-- ===============================
     6. 상세 페이지 로그인 기반 조회기록 저장
     =============================== -->
	<insert id="insertViewHistory" parameterType="map">
	
	    INSERT INTO room_view_history
	    VALUES (
	        seq_room_view_history.NEXTVAL,
	        #{memberNo},
	        #{roomId},
	        SYSDATE
	    )
	
	</insert>
	
	
	<!-- ===============================
     7. 상세 페이지 로그인 기반 추천 객실 조회
     =============================== -->
	<select id="selectRecommendedRooms"
	        parameterType="map"
	        resultType="RoomTypeDTO">
	
	    SELECT *
	    FROM (
	        SELECT rt.*
	        FROM room_type rt
	        WHERE rt.room_type_id != #{currentRoomId}
	          AND rt.room_grade IN (
	                SELECT DISTINCT r.room_grade
	                FROM room_view_history vh
	                JOIN room_type r
	                  ON vh.room_type_id = r.room_type_id
	                WHERE vh.member_no = #{memberNo}
	          )
	        ORDER BY rt.base_price DESC
	    )
	    WHERE ROWNUM &lt;= 3
	
	</select>
	
</mapper>